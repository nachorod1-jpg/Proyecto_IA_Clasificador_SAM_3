# Ruta a la carpeta o archivo de checkpoints de SAM-3 (no se versiona)
SAM3_WEIGHTS_DIR=/ruta/a/pesos
# Alternativamente, ruta directa a un checkpoint
SAM3_CHECKPOINT_PATH=/ruta/a/checkpoint/sam3.ckpt
# Ajustes opcionales
DATABASE_PATH=data/app.db
LOGS_DIR=logs
OUTPUT_DIR=output
